# Continuation prompt

Resume the `podcasts-swimming-processor` project from its canonical GitHub repository and Mac checkout. Read `STATUS.md` first, then `PLAN.md`, then the latest `WORKLOG.md`. Inspect actual repository and workspace-coordinator state before acting; do not restart completed work. Goal: finish a localhost macOS web app for selecting downloaded Apple Podcasts episodes and reconciling them to the Mojawa Run Plus at `/Volumes/RUN PLUS`. Selected episodes must be speech-compressed, loudness-normalised/limited, encoded to MP3 and split to 10-minute chunks by default; unselected episodes must have all and only app-managed chunks removed. Include Normal/Swim/Aggressive presets, configurable segment length, 30-second preview, search/filter UI, progress, safe eject, manifest-driven ownership, interruption-safe copy/atomic state, no Apple Podcasts writes, no unrelated-device deletion, and no runtime dependency beyond FFmpeg plus standard-library Python. Use the mac-git-bridge durable workspace coordinator for substantial work, checkpoint/push coherent progress, validate before integration, integrate to main, and independently verify the GitHub remote. Never replay an ambiguous mutation; inspect real state first. Completion requires all tests and adversarial safety gates to pass and `STATUS.md` to say COMPLETE.
