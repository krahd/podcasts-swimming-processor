# podcasts-swimming-processor

Local macOS utility for preparing downloaded Apple Podcasts episodes for swimming headphones. The application is being implemented with a browser interface, speech-oriented audio processing, configurable track segmentation, and safe manifest-driven synchronisation to a Mojawa Run Plus.

See `PLAN.md` and `STATUS.md` for the current implementation state.
